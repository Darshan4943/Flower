{
    "name": "Shivani",
    "version": "1.0.0",
    "description": "Description of your project",
    "scripts": {
        "build": "rmdir /s /q dist > nul 2>&1 && mkdir dist && xcopy index.html dist\\ && xcopy style.css dist\\ && xcopy app.js dist\\"
      }
  }
  