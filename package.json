{
    "name": "Shivani",
    "version": "1.0.0",
    "description": "Description of your project",
    "scripts": {
        "build": "if exist dist rmdir /s /q dist && mkdir dist && xcopy index.html dist\\ && xcopy style.css dist\\ && xcopy app.js dist\\ || mkdir dist && xcopy index.html dist\\ && xcopy style.css dist\\ && xcopy app.js dist\\"
      }
  }
  