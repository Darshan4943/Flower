{
    "name": "Shivani",
    "version": "1.0.0",
    "description": "Description of your project",
    "scripts": {
        "build": "rm -rf dist && mkdir dist && cp index.html dist/ && cp style.css dist/ && cp app.js dist/"
      }
  }
  