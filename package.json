{
    "name": "Shivani",
    "version": "1.0.0",
    "description": "Description of your project",
    "scripts": {
      "build": "if exist dist ( rd /s /q dist ) else ( mkdir dist ) && xcopy index.html dist && xcopy style.css dist && xcopy app.js dist"
    }
  }
  